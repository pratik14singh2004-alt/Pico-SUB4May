<ContentDialog
    x:Class="DSPiConsole.Dialogs.AutoEQBrowserDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="AutoEQ Profile Browser"
    PrimaryButtonText="Apply"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False">

    <Grid Width="500" Height="450">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <AutoSuggestBox x:Name="SearchBox"
                        Grid.Row="0"
                        PlaceholderText="Search headphones..."
                        QueryIcon="Find"
                        Margin="0,0,0,12"
                        TextChanged="OnSearchTextChanged"/>

        <!-- Results List -->
        <ListView x:Name="ResultsList"
                  Grid.Row="1"
                  SelectionMode="Single"
                  SelectionChanged="OnSelectionChanged">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Selected Profile Info -->
        <Border x:Name="SelectedInfoPanel"
                Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                CornerRadius="4"
                Padding="12"
                Margin="0,12,0,0"
                Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="4">
                    <TextBlock x:Name="SelectedNameText" FontWeight="SemiBold"/>
                    <TextBlock x:Name="SelectedDetailsText"
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           x:Name="PreampText"
                           FontFamily="Cascadia Code, Consolas"
                           VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
        </Border>
    </Grid>
</ContentDialog>
